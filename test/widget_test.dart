// Placeholder — widget tests require a running device/emulator with ML Kit.
// Integration tests are handled separately.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () => expect(true, isTrue));
}
